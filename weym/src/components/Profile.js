import React, { Component } from 'react';

class Profile extends Component {
    constructor(props) {
        super(props);
        this.state = {
            // Initialize state here
        };
    }

    componentDidMount() {
        // Code to run after component has mounted goes here
    }

    componentDidUpdate(prevProps, prevState) {
        // Code to run after component has updated goes here
    }

    componentWillUnmount() {
        // Code to run just before component is unmounted goes here
    }

    render() {
        return (
            <h1>profile</h1>
        );
    }
}

export default Profile;